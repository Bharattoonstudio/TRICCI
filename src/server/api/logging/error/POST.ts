@export default async function handler(req, res) { if (req.method !== "POST") return res.status(405).json({ message: "Method not allowed" }); try { const { message, stack, url, timestamp } = req.body; console.error("[CLIENT_ERROR]", { message, stack, url, timestamp: new Date(timestamp).toISOString(), userAgent: req.headers["user-agent"] }); return res.status(200).json({ success: true }); } catch (error) { console.error("logs.error.handler", error); return res.status(500).json({ message: "Failed to log error" }); } }@
